# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: 2026 NeoFOAM authors
#
# The incompressibleFluid drop-in study. Everything solver-specific lives beside
# this file in config.yaml (title, tutorial group, `only:` subset) and discover.py
# (tier classification); the pipeline itself is the shared, packaged driver.
#
# Run from this directory:
#     snakemake --configfile config.yaml -j6

from neofoam.tooling.verification.rules import rules_dir

include: str(rules_dir() / "driver.smk")

# The default target must be defined here, in the main Snakefile — Snakemake never
# promotes a rule from an included file to the default. REPORT comes from driver.smk.
rule all:
    input:
        REPORT,
