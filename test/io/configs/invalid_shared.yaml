# Invalid shared config - missing required field "port" and has invalid "poolSize": -5 (should be gt=0)
config:
  service_a:
    timeout: 30
    maxConnections: 10

  service_b:
    endpoint: example.com
    poolSize: -5

  service_c:
    enabled: true
    bufferSize: 1024
