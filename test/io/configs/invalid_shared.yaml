# Invalid shared config - missing required field "port" in database subdict
settings:
  general:
    timeout: 30
    retries: 3

  database:
    host: localhost
    name: mydb

  cache:
    enabled: true
    ttl: 600
