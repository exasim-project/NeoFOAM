# Invalid nested config - missing required field "version" in application subdict
application:
  name: solver

solver:
  linear:
    method: PCG
    tolerance: 1.0e-6
    maxIterations: 1000
  nonlinear:
    method: Newton
    tolerance: 1.0e-8
    maxIterations: 50
