# Invalid nested config - missing required field "version" in metadata subdict
# Also has invalid value: priority is 0 (should be gt=0)
metadata:
  name: TestApp
  priority: 0

processing:
  stage1:
    algorithm: fast
    threshold: 0.001
    maxIterations: 100
    batchSize: 32
  stage2:
    algorithm: accurate
    threshold: 0.0001
    maxIterations: 50
    batchSize: 16
