# An unknown key of the file itself: ``table`` instead of ``tables``, which
# would otherwise read as a case that declares nothing.
table:
  - name: typo_file
    source: {type: internal, field: p}
    pipeline: [{type: volIntegrate, name: typo_file}]
