# An unknown key of the table itself: ``pipelines`` instead of ``pipeline``,
# which would otherwise leave the table with the default (empty) pipeline.
tables:
  - name: typo_table
    source: {type: internal, field: p}
    pipelines: [{type: volIntegrate, name: typo_table}]
