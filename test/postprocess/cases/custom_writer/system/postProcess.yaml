# ``text`` is not a NeoFOAM writer: this case's postProcess.py registers it.
tables:
  - name: volume_p_declared
    source: {type: internal, field: p}
    pipeline: [{type: volIntegrate, name: volume_p_declared}]
    writer: {type: text}
