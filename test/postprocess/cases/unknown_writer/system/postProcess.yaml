# ``nope`` is no writer at all: resolving this table must say so.
tables:
  - name: volume_p
    source: {type: internal, field: p}
    pipeline: [{type: volIntegrate, name: volume_p}]
    writer: {type: nope}
