# The volume integral of p, written every step (the default cadence).
# postProcess.json in this directory is the same tree in the other format.
tables:
  - name: volume_p
    source: {type: internal, field: p}
    pipeline: [{type: volIntegrate, name: volume_p}]
