// -*- C++ -*-
// Pulled into system/controlDict by `#includeFunc writeFields`. The name is
// deliberately not one of OpenFOAM's own $FOAM_ETC/caseDicts functions, so it can
// only be found through <system>, i.e. through $FOAM_CASE.

type            writeObjects;
libs            (utilityFunctionObjects);
objects         ();
writeControl    writeTime;

// ************************************************************************* //
