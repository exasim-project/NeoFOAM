FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      fvSolution;
}

solvers
{
    U
    {
        ibm         directForcing;
        backend     jax;
    }
    p
    {
        solver      MLMG;
        rtol        1e-8;
        atol        1e-11;
        maxIter     400;
        // Krylov bottom solver: the slip-wall outflow nodal projection converges in
        // ~5 V-cycles with it (vs ~600 for "smoother") — ~5.5x faster overall.
        bottomSolver bicgstab;
    }
}

blockAMR
{
    nu          0.01;      // Re = U*D/nu = 1*0.2/0.01 = 20
}
