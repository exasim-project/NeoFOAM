# The volume integral of p, appended every time step (the default cadence).
# ../postprocess_script/system/postProcess.py declares the same table in Python.
tables:
  - name: volume_p
    source: {type: internal, field: p}
    pipeline: [{type: volIntegrate, name: volume_p}]
