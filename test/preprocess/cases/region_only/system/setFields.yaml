# A field named in a region but in no default: the cells outside the region keep
# what cases/box has on disk.
regions:
  - region: {type: box, min: [0, 0, -1], max: [0.5, 1, 1]}
    values: {alpha.water: 1}
