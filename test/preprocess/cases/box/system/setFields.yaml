# The unit box is cut into 4x4x1 cells, so the region below is the two
# left-hand cell columns — eight of the sixteen cells.
defaults:
  alpha.water: 0
  U: [0, 0, 0]
regions:
  - region: {type: box, min: [0, 0, -1], max: [0.5, 1, 1]}
    values: {alpha.water: 1, U: [0.5, 0, 0]}
